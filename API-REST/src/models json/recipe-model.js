'use strict'

var conn = require('./mongo-connect'),
    RecipeModel = () => {}

    RecipeModel.getAll = (cb) => {}

    RecipeModel.getOne = (id, cb) => {}

    RecipeModel.save = (data, cb) => {}

    RecipeModel.delete = (id, cb) => {}

    module.export  = RecipeModel